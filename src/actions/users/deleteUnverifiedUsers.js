exports.handler = async (event) => {
  if (event.keep_alive_request) { return {}; }
  //TODO: find old unverified accounts
  //TODO: delete accounts
  //TODO: create response + log actions
  return {}
};
